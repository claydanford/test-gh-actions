name: login

on: [push]

jobs:
  login:
    runs-on: ubuntu-latest
    outputs:
      token: ${{ steps.login.outputs.test }}
    steps:
      - id: login
        run: echo "::set-output name=test::foo"
